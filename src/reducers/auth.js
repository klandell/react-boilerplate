const initialState = {};

const auth = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export const selectUserName = state => {
  // return a property
  return state;
};

export default auth;
